
  # Event management system (Community)

  This is a code bundle for Event management system (Community). The original project is available at https://www.figma.com/design/YX4MVL80XyEZeGLGMylHsg/Event-management-system--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
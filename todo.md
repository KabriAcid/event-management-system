<!--  
This project uses React (Typescript) as the frontend and PHP as the backend. The backend isn't designed nor created yet.
    As for the backend, I expect to see a standard, robust, API-driven approach. Where we have backend routes that returns JSON objects and then the frontend React utilizes and propagates properly. Also, KPIs can have their separate 
  -->
** ORGANIZER **
- Set routes for all necessary pages.
- onClick create events button, creates a new route hence the create event button in the organizer sidebar works properly also.
- Events view details page in /organizer/events
- Events card ellipsis button CRUD functionality in /organizer/events
- Ellipsis button functionality in /organizer/attendees
- Organizer Security tab in /organizer/settings page.
- Remove 'billing' tab.

** ATTENDEE **
- Search functionality UI needs visibility
- Mobile responsiveness for the attendee navigation in /attendee
- Create View tickets page
- Remove the 'add to calendar' button in the /tickets page.

** GENERAL ** 
- Remove all unnecessary nav links in footer since this is a final year project not real-live.
- Add some nice font-face.
- A detailed document entailing the contents of the project so that the documentation should match.